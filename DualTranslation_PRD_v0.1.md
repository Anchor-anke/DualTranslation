# DualTranslation（二元编译）产品需求文档

> 在用户与 Coding Agent 之间，完成“写得清楚”和“看得明白”的双向转换。

| 文档属性 | 内容 |
|---|---|
| 文档类型 | Product Requirements Document（PRD） |
| 版本 | v0.1 |
| 状态 | MVP 范围已确认 |
| 日期 | 2026-08-03 |
| 首期范围 | macOS 悬浮窗/菜单栏应用；提示词完善与 Agent 回复解释 |
| 产品名称 | DualTranslation |
| 中文名称 | 二元编译 |

## 1. 执行摘要

DualTranslation 是一个运行在 macOS 上的轻量悬浮式沟通助手。它位于用户与 Coding Agent 之间，提供“写给 Agent”和“看懂 Agent”两个模式：前者把用户模糊、不完整的想法整理成清晰、可执行的提示词；后者把用户主动提供的 Agent 回复转换成更通俗、易理解的说明。

产品不把“中文翻译成英文”视为固定目标。研究和公开评测表明，部分模型在部分任务上的英文表现确实更高，但这种优势并不稳定，且受到模型、任务、评测来源与翻译损失影响。因此，DualTranslation 采用“语义编译 + 语言路由”的双层结构，在中文、英文和中英混合表达之间动态选择。

首期产品聚焦个人开发者的高频轻量场景，通过全局快捷键唤起悬浮窗，支持手动输入或由用户主动读取剪贴板。转换结果由用户检查并一键复制，再自行粘贴到 Cursor、Codex 或其他 Coding Agent。产品不读取代码仓库、不自动操作 Agent，也不持续监控其他应用。

**核心产品判断：** 第一版首先验证用户是否愿意频繁使用“双向转换”，而不是提前解决跨应用自动化、仓库上下文读取或 Agent 编排问题。

## 2. 背景与问题

### 2.1 用户现状

用户在使用 Cursor、Codex 等编码 Agent 时，常以接近日常交流的方式描述任务，例如：

> 帮我把这个页面弄得更高级一点，顺便把结构优化一下。

这类表达对人类合作者可能足够，但对 Agent 存在明显缺口：

- “高级”的可观察含义不明确；
- “结构”可能指代码结构、页面信息架构或组件层级；
- 没有说明哪些行为必须保留；
- 没有定义允许修改的文件范围和技术约束；
- 没有验收标准与验证方式；
- Agent 不知道应该直接实现，还是先提出方案。

用户真正需要的并不是把一句话扩写得更长，而是减少执行结果偏离原始意图的概率。

沟通的另一方向同样存在问题：Coding Agent 的回复往往包含实现术语、命令、文件路径、测试结果和大量过程信息。非资深用户难以快速判断任务是否完成、哪些内容尚未验证、自己是否还需要做出决定。单纯压缩回复又可能丢失错误、警告和风险，因此产品需要在“更通俗”和“保留技术事实”之间保持边界。

### 2.2 语言差异的事实边界

公开证据支持“部分模型、部分任务存在英文优势”，但不支持“任何提示词翻译成英文都会更准确”。例如，OpenAI 公布的多语言 MMLU 测试中，o1 的英文得分为 92.30%，简体中文为 88.92%；GPT-4o 的英文得分为 88.70%，简体中文为 83.35%。该测试由英文原始数据翻译而来，因此不能排除原始语言优势。

NAACL 2025 的研究在控制“翻译腔”后发现，英文指令的优势并不压倒性。2026 年的 ChLogic 中英文对齐逻辑评测则发现，把中文回译成英文有时可以改善普通任务，但在困难任务上效果因模型而异，部分模型翻译后反而变差。EMNLP 2025 的研究还发现，在特定中英双语推理模型中，强制单语解码会降低准确率，说明混合语言有时可能是有效策略。

由此得到三项产品约束：

1. 不把中文输入机械转换为英文；
2. 不把推测出的需求伪装成用户原始要求；
3. 根据模型、任务和语义损失动态选择输出语言。

### 2.3 现有替代方案的不足

用户当前通常采用以下方式：

- 自己学习 Prompt Engineering 模板；
- 让通用聊天模型“优化一下这段提示词”；
- 复制网络上的超长系统提示词；
- 反复试错，直到 Agent 输出接近预期；
- 手动将中文翻译成英文后再次执行；
- 自己从冗长的 Agent 回复中寻找结论、验证结果和下一步。

这些方式普遍缺乏任务级歧义判断、目标 Agent 适配、假设可见性和可度量的验收标准。

## 3. 产品愿景与定位

### 3.1 产品愿景

让用户用自己最自然的方式与 Coding Agent 沟通：既能把想法说清楚，也能快速看懂 Agent 做了什么。

### 3.2 产品定位

DualTranslation 是连接“人类表达”和“Agent 表达”的 macOS 轻量工具，而不是 Coding Agent、代码执行器或跨应用自动化平台。

### 3.3 “二元编译”的两条方向

**人到 Agent：写得清楚**

将模糊、隐含和不完整的想法转换为明确的目标、上下文、范围、约束和验收标准。

**Agent 到人：看得明白**

将复杂、技术化或冗长的 Agent 回复转换为通俗说明，同时保留命令、代码、路径、错误、风险和未验证事项等关键信息。

### 3.4 价值主张

> DualTranslation 帮用户把需求写成 Agent 容易执行的形式，也把 Agent 的回复解释成人容易理解的形式。

## 4. 目标用户与核心场景

### 4.1 目标用户

**主要用户：AI 辅助开发者**

- 经常使用 Cursor、Codex 或类似编码 Agent；
- 能表达产品或代码目标，但不擅长写完整任务规格；
- 关心结果质量，希望减少往返沟通和无效修改。

**次要用户：非技术产品创造者**

- 能描述想做的产品，但不熟悉工程术语；
- 需要把产品想法转换为开发 Agent 可以理解的任务；
- 容易在技术方案和需求目标之间混淆。

**高级用户：Agent 工作流设计者**

- 已有成熟提示词习惯；
- 需要快速生成不同 Agent 的适配版本；
- 希望通过本地历史快速复用和调整高质量表达。

### 4.2 核心 Jobs to Be Done

- 当我只有一个模糊想法时，帮我明确真正想实现的结果。
- 当关键信息缺失时，只问最有价值的问题，不要进行冗长访谈。
- 当我准备把任务交给编码 Agent 时，生成可以直接粘贴执行的任务说明。
- 当同一任务面向不同 Agent 时，帮我调整表达重点和交付要求。
- 当英文可能更有效但可能损失中文含义时，保留我的原始语义并选择合适语言。
- 当 Agent 回复过于技术化或冗长时，告诉我它做了什么、是否成功、有什么风险以及下一步需要什么。
- 当我频繁切换应用时，让我通过全局快捷键快速完成输入、转换和复制。

### 4.3 首期核心场景

1. 把功能想法、Bug 描述、UI 调整或重构需求整理成清晰 Prompt；
2. 把 Cursor、Codex 或其他 Coding Agent 的回复解释成通俗说明；
3. 根据 Codex、Cursor 或通用 Agent 调整输出表达；
4. 在中文、英文和双语输出之间快速切换；
5. 从本地历史中找回最近的转换结果并再次复制。

## 5. 产品目标与非目标

### 5.1 产品目标

- 将模糊需求转换为结构完整、可执行、可验证的任务规格；
- 将 Agent 的复杂回复转换为通俗说明，并保留关键技术事实；
- 将关键假设和未确认项显式展示给用户；
- 通过悬浮窗、菜单栏入口和全局快捷键降低使用摩擦；
- 支持手动输入和用户主动读取剪贴板；
- 为 Cursor、Codex 和通用 Agent 提供轻量表达适配；
- 支持中文、英文和中英混合三种输出策略；
- 提供一键复制、本地历史记录和敏感信息提醒。

### 5.2 非目标

首期不包含：

- 读取整个代码仓库；
- 自动操作 Cursor 或 Codex；
- 自动发送提示词；
- 自动监控所有 Agent 回复；
- 多 Agent 调度；
- 团队协作；
- 复杂的 A/B 测试系统；
- 直接执行任何代码或终端命令。

## 6. 产品原则

1. **准确优先于冗长。** 提示词长度不是质量指标。
2. **保留意图来源。** 用户原始需求是语义事实源。
3. **不隐藏假设。** 新增内容必须标记为推断、建议或默认值。
4. **最少必要提问。** 只询问会显著改变结果的关键问题。
5. **结果优先，步骤适度。** 明确目标和约束，不无谓限制 Agent 的实现空间。
6. **验收必须可观察。** 避免“更好”“更高级”“更加优雅”等不可验证标准。
7. **语言由任务决定。** 不把英文设为固定的高级模式。
8. **适配保持轻量。** 使用统一任务协议加少量 Agent 适配，避免维护多套独立体系。
9. **读取必须由用户触发。** 不后台监听剪贴板或其他应用内容。
10. **用户保留最终控制。** 产品只负责转换和复制，不自动发送、操作或执行。

## 7. 核心产品流程

### 7.1 启动与输入

```text
菜单栏入口或全局快捷键
          ↓
打开悬浮窗
          ↓
手动输入 / 用户点击“读取剪贴板”
          ↓
选择“写给 Agent”或“看懂 Agent”
          ↓
选择目标 Agent 与输出语言
          ↓
转换、检查并一键复制
```

应用不得在用户未触发时读取剪贴板，不得持续监听 Cursor、Codex 或其他应用窗口。

### 7.2 “写给 Agent”流程

```text
用户输入模糊想法
        ↓
识别目标、范围、约束和关键歧义
        ↓
必要时提出最少量问题 / 否则显式列出安全假设
        ↓
生成统一任务规格（Canonical Task Spec）
        ↓
应用目标 Agent 和语言选项
        ↓
输出完整 Prompt、关键假设和验收标准
        ↓
用户一键复制
```

### 7.3 “看懂 Agent”流程

```text
用户手动输入或主动读取剪贴板中的 Agent 回复
        ↓
识别结论、已执行动作、验证结果、风险和待确认事项
        ↓
保留原始命令、代码、路径、错误和警告
        ↓
转换为目标语言的通俗说明
        ↓
用户一键复制或返回 Agent 继续沟通
```

### 7.4 缺失信息分类

| 类型 | 判断标准 | 系统行为 |
|---|---|---|
| 必须询问 | 不同答案会显著改变范围、架构、风险或交付结果 | 在生成前提问，默认不超过 3 个 |
| 安全假设 | 风险低、可逆，且存在合理默认值 | 继续生成，并显式列出假设 |
| Agent 探索 | 信息可由 Coding Agent 在执行时检查 | 写入“先检查再实施”指令，DualTranslation 自身不读取仓库 |
| 暂不需要 | 不影响当前任务完成 | 不询问、不加入任务规格 |

### 7.5 生成策略

**快速模式**

- 适用于低风险、小范围和可逆任务；
- 系统做合理假设后直接生成；
- 用户可以要求“不要问，直接生成”。

**协商模式**

- 适用于新功能、架构改造、数据迁移和大范围设计调整；
- 一轮提出最多 3 个关键问题；
- 回答后生成任务包，必要时再进行一轮确认。

系统默认自动选择模式，同时允许用户手动切换。

## 8. 功能需求

### 8.1 需求总览

| 编号 | 功能 | 优先级 | MVP |
|---|---|---:|:---:|
| FR-01 | macOS 菜单栏入口与悬浮窗 | P0 | 是 |
| FR-02 | 可配置的全局快捷键 | P0 | 是 |
| FR-03 | 手动输入与用户主动读取剪贴板 | P0 | 是 |
| FR-04 | “写给 Agent”和“看懂 Agent”双模式 | P0 | 是 |
| FR-05 | 模糊需求解析、歧义检测与关键假设 | P0 | 是 |
| FR-06 | Agent 回复的通俗解释与关键信息保留 | P0 | 是 |
| FR-07 | 中文、英文和双语输出 | P0 | 是 |
| FR-08 | Codex、Cursor 和通用 Agent 目标选项 | P0 | 是 |
| FR-09 | 一键复制转换结果 | P0 | 是 |
| FR-10 | 最近转换记录的本地历史 | P0 | 是 |
| FR-11 | 敏感信息检测与提醒 | P0 | 是 |
| FR-12 | 快速模式、协商模式与自然语言调整 | P1 | 是 |
| FR-13 | 读取项目上下文或代码仓库 | P2 | 否 |
| FR-14 | 自动监控或操作 Coding Agent | P2 | 否 |
| FR-15 | 团队协作、A/B 测试和多 Agent 调度 | P2 | 否 |

### 8.2 macOS 应用外壳

- 应用常驻 macOS 菜单栏；
- 用户可以通过菜单栏图标或全局快捷键打开/隐藏悬浮窗；
- 悬浮窗默认位于当前工作区前方，但不抢占用户正在输入的内容；
- 用户可以拖动窗口并记住上次位置；
- 全局快捷键允许用户在设置中修改，避免与其他应用冲突。

### 8.3 输入与读取原则

- 用户可以直接在悬浮窗内手动输入；
- 用户点击“读取剪贴板”后，应用才可以访问当前剪贴板文本；
- 应用不得在后台持续监听剪贴板；
- 应用不得自动读取 Cursor、Codex、代码编辑器、终端或其他应用窗口；
- 不支持的剪贴板内容类型应提示用户，不得静默上传；
- 读取成功后，用户可以在转换前查看和修改原文。

### 8.4 意图解析器

系统必须从输入中提取：

- 用户想要的最终结果；
- 任务对象与作用范围；
- 用户这样做的动机或业务目标；
- 已明确的限制条件；
- 隐含但尚未确认的前提；
- 期望 Agent 执行的动作类型；
- 可用于验收结果的线索。

系统应区分“用户要求”和“系统建议”，不得将系统建议合并成用户事实。

### 8.5 歧义检测与澄清

系统必须对歧义按照影响程度排序，优先处理：

1. 会改变产品行为或业务逻辑的问题；
2. 会导致不同技术架构的问题；
3. 涉及数据丢失、安全或外部影响的问题；
4. 会显著扩大修改范围的问题；
5. 仅影响偏好和样式的问题。

单轮澄清问题默认不超过 3 个。每个问题应说明为什么需要确认，或者通过选项展示不同答案的影响。

### 8.6 “写给 Agent”任务规格

内部应生成平台无关的 Canonical Task Spec，至少包括：

```yaml
task:
  title: string
  type: feature | bugfix | ui | refactor | review | research
  goal: string
  motivation: string | null
  context: [string]
  scope:
    in_scope: [string]
    out_of_scope: [string]
  constraints: [string]
  assumptions: [string]
  unknowns: [string]
  agent_behavior:
    inspect_before_action: boolean
    plan_before_action: boolean
    confirmation_required_for: [string]
  acceptance_criteria: [string]
  verification: [string]
  deliverables: [string]
  output_language: zh | en | bilingual
  target_agent: generic | cursor | codex
```

该规格是产品的核心中间表示。用户界面可以隐藏部分字段，但系统必须保留可追踪结构。

### 8.7 “看懂 Agent”解释规格

系统应从 Agent 回复中提取：

```yaml
explanation:
  summary: string
  status: completed | partial | failed | unclear
  actions_taken: [string]
  verification_results: [string]
  user_decisions_needed: [string]
  risks_and_warnings: [string]
  suggested_next_steps: [string]
  preserved_technical_details:
    commands: [string]
    file_paths: [string]
    errors: [string]
    code_snippets: [string]
  output_language: zh | en | bilingual
```

通俗解释不得把“尚未验证”改写为“已经成功”，也不得修改原始命令、路径、代码、错误信息或警告的含义。

### 8.8 语言路由器

系统支持三种策略：

**Chinese-native**

- 任务依赖中文语义、中文文案或本地文化；
- 目标模型中文能力较强；
- 英译会造成明显信息损失。

**English-optimized**

- 编程、API、英文错误信息或英文文档密集；
- 关键工程术语以英文表达更稳定；
- 目标模型在同类任务的英文历史表现更好。

**Bilingual-preserving**

- 执行指令适合英文，但用户意图包含难以直译的中文概念；
- 需要保留中文语义锚点；
- 模型或任务可能受益于双语表达。

系统必须展示当前选择和简短理由，并允许用户一键切换。首期语言路由以规则和模型提示结合实现，不承诺完全自动最优。

### 8.9 Agent 目标选项

统一任务规格生成后，目标选项只调整与表达和交付相关的内容：

- 是否应先提出方案；
- 测试、构建和验证要求；
- 完成后的报告格式。

首期提供：

- Cursor 适配版；
- Codex 适配版；
- 通用 Agent 版。

目标选项不得改变用户目标或自行加入重大技术决策。

### 8.10 转换结果输出

“写给 Agent”模式至少展示：

1. **可直接使用的 Prompt**：可一键复制；
2. **假设与未确认项**：清楚区分系统推断；
3. **验收标准**：描述怎样判断完成；
4. **生成说明**：目标 Agent、语言策略及选择理由。

“看懂 Agent”模式至少展示：

1. **一句话结论**：Agent 当前处于完成、部分完成、失败或无法判断中的哪种状态；
2. **Agent 做了什么**：用通俗语言说明主要动作；
3. **需要用户处理的事项**：决策、确认或补充信息；
4. **风险与下一步**：保留未验证事项和警告；
5. **原始技术信息**：可展开查看命令、路径、错误和代码片段。

复杂任务可额外展示“建议执行策略”和“范围外事项”。简单任务应自动压缩，避免产生超长 Prompt。

### 8.11 迭代调整

用户可以通过自然语言调整任务包，例如：

- 更简洁；
- 更自主，减少提问；
- 先规划后执行；
- 不要改代码，只分析；
- 限制在当前文件；
- 改成英文；
- 保留中文 UI 文案；
- 加入测试和回滚要求。

每次调整应保留版本，明确哪些字段发生变化。

### 8.12 本地历史记录

- 保存最近的转换类型、原始输入、转换结果、目标 Agent、输出语言和时间；
- 历史记录默认只保存在本机；
- 用户可以查看、重新复制、删除单条记录或清空全部历史；
- MVP 建议默认保留最近 20 条，具体数量在设置中可调整；
- 敏感信息命中的记录默认不进入历史，除非用户明确选择保存。

### 8.13 敏感信息提醒

- 在提交转换前检测常见 API Key、访问令牌、私钥、密码和高风险个人信息模式；
- 命中后提示用户检查或遮盖，不直接替用户判断内容一定敏感；
- 用户可以取消转换、遮盖后继续，或明确确认继续；
- 提醒不能修改代码、命令或路径的原始含义。

## 9. 交互设计

### 9.1 MVP 页面结构

```text
┌──────────────────────────────────────────────┐
│ DualTranslation / 二元编译          历史  设置 │
├──────────────────────────────────────────────┤
│ [写给 Agent]          [看懂 Agent]            │
├──────────────────────────────────────────────┤
│ 输入或粘贴内容……                             │
│ [                                            ]│
│ [                                            ]│
│                         [读取剪贴板]           │
│                                              │
│ 目标：Codex ▼      输出：中文 ▼               │
│                         [开始转换]             │
├──────────────────────────────────────────────┤
│ ⚠ 敏感信息提醒（命中时显示）                  │
│                                              │
│ 转换结果                                     │
│ ……                                           │
│                                              │
│ 关键假设 / 风险 / 原始技术信息                │
│                         [一键复制]             │
└──────────────────────────────────────────────┘
```

### 9.2 “写给 Agent”示例

用户输入：

> 我想给项目加个登录，最好简单一点。

系统识别的关键歧义：

- 使用项目现有后端自行保存账号，还是允许接入第三方认证服务？
- “简单”是指首期只支持邮箱密码，还是也包含注册和找回密码？

系统在获得回答后生成：

- 目标与范围明确的 Prompt；
- 保留现有技术栈、不明文保存密码等约束；
- 登录、退出、受保护路由等验收标准；
- 运行测试、类型检查和构建的验证要求；
- 当前未包含找回密码或第三方登录的范围说明。

用户检查结果后点击“一键复制”，自行粘贴并发送给 Coding Agent。

### 9.3 “看懂 Agent”示例

用户在 Cursor 或 Codex 中复制一段技术回复，打开 DualTranslation，切换到“看懂 Agent”，并主动点击“读取剪贴板”。

系统输出：

- Agent 是否已完成任务；
- 它修改或检查了什么；
- 已执行和未执行的验证；
- 需要用户决定或补充的事项；
- 风险、警告和建议下一步；
- 可展开查看的原始命令、路径、错误和代码片段。

## 10. 非功能需求

### 10.1 响应与可用性

- 全局快捷键应能在常用应用前台时稳定唤起悬浮窗；
- 快速模式首屏结果目标在 5 秒内可见；
- 协商模式首轮问题目标在 3 秒内可见；
- 用户不应被要求填写完整表单后才能获得价值；
- 任何生成结果均可复制为纯文本；
- “读取剪贴板”和“一键复制”必须具有明确的即时反馈。

### 10.2 可解释性

- 必须区分用户要求、系统假设和系统建议；
- 必须展示语言策略和目标 Agent；
- 对高影响新增约束提供生成理由；
- “看懂 Agent”模式必须区分事实、推断、未验证事项和建议；
- 用户可以查看结构化任务规格或保留的原始技术信息。

### 10.3 隐私与安全

- 只有用户点击“读取剪贴板”时才访问剪贴板内容；
- 不后台监听剪贴板，不自动读取其他应用窗口；
- 不读取代码仓库、终端历史或 Cursor/Codex 内部数据；
- 对密钥、令牌、密码、私钥、个人信息和内部代码提供敏感信息提醒；
- 历史记录默认本地保存，支持单条删除和全部清空；
- 敏感信息命中的内容默认不进入历史；
- 不自动发送提示词，不执行代码、命令或外部发布操作。

### 10.4 可扩展性

- 统一任务规格与 Agent 适配器解耦；
- “写给 Agent”和“看懂 Agent”共享输入、语言、历史和敏感信息基础能力；
- 新增模型或 Agent 时，不重写意图解析流程；
- 语言策略支持基于模型版本和任务类型更新；
- macOS 应用外壳与转换服务解耦，便于未来扩展其他平台。

## 11. 成功指标

### 11.1 北极星指标

**转换结果直接采用率（Direct Adoption Rate）**

用户生成结果后直接复制，或只做轻微修改后复制使用的转换次数占比。该指标同时覆盖“写给 Agent”和“看懂 Agent”两个模式。

### 11.2 核心指标

| 指标 | 定义 | MVP 目标 |
|---|---|---:|
| 转换结果直接采用率 | 生成后直接复制或仅轻微编辑 | ≥ 70% |
| Prompt 接受率 | “写给 Agent”结果被复制使用 | ≥ 70% |
| 解释有效率 | 用户认为通俗解释帮助其理解原回复 | ≥ 75% |
| 核心意图改写率 | 用户需要修正目标、范围或关键约束 | ≤ 20% |
| 平均澄清问题数 | 每个进入协商模式的任务 | ≤ 3 |
| 中位转换时间 | 从打开悬浮窗到复制结果 | ≤ 60 秒 |
| 快捷键唤起成功率 | 快捷键触发后窗口正常出现 | ≥ 99% |
| 假设纠错率 | 用户否定系统关键假设的任务占比 | 持续下降 |

这些数值是 MVP 阶段的产品假设，需要通过真实用户测试校准。

### 11.3 诊断指标

- 中文、英文和双语版本的成功率差异；
- 不同目标 Agent 的成功率；
- 两种模式的使用频率和留存差异；
- 手动输入与主动读取剪贴板的使用比例；
- 因范围遗漏、约束冲突、语言损失或解释失真导致的失败比例；
- 用户主动切换语言策略的频率；
- 用户删除系统新增要求的频率；
- 敏感信息提醒的触发、取消和继续比例；
- 历史记录的再次复制和清理频率。

## 12. MVP 范围与版本规划

### 12.1 MVP（v0.1）

- macOS 悬浮窗或菜单栏应用；
- 全局快捷键；
- 手动输入；
- 用户主动读取剪贴板；
- “写给 Agent”和“看懂 Agent”两个模式；
- Codex、Cursor、通用 Agent 三个目标选项；
- 中文、英文和双语输出；
- 显示关键假设；
- 一键复制；
- 最近若干条本地历史记录；
- 敏感信息提醒；
- 用户主动触发时才读取内容。

### 12.2 MVP 明确排除

- 读取整个代码仓库；
- 自动操作 Cursor 或 Codex；
- 自动发送提示词；
- 自动监控所有 Agent 回复；
- 多 Agent 调度；
- 团队协作；
- 复杂的 A/B 测试系统；
- 直接执行任何代码或终端命令。

### 12.3 后续候选能力

- 搜索、收藏和标签化本地历史；
- 个人常用约束、语气和输出偏好；
- 更多 Coding Agent 目标模板；
- 同一输入的中文、英文和双语对照查看；
- 更细致的失败原因反馈与二次转换；
- 自动读取或操作其他应用只作为独立验证方向，不默认进入下一版本。

## 13. 风险与应对

| 风险 | 影响 | 应对策略 |
|---|---|---|
| 系统自信地发明需求 | Prompt 看似完整但偏离用户目标 | 强制区分要求、假设和建议；高影响假设必须确认 |
| Prompt 过长 | 增加阅读成本并稀释关键指令 | 按任务复杂度控制长度；默认展示精简版 |
| 机械英译造成语义损失 | 中文审美、文化和业务语义被扭曲 | 保留中文语义源；支持双语锚点和一键切换 |
| 语言路由缺乏稳定依据 | 用户误以为英文一定更强 | 展示选择理由和置信度；基于真实反馈迭代 |
| 过度规定实现步骤 | 限制强 Agent 自主探索 | 优先定义结果、约束和验收，步骤只在必要时添加 |
| 不同 Agent 更新频繁 | 适配规则很快过时 | 适配层独立版本化；定期小样本回归测试 |
| 通俗解释遗漏技术风险 | 用户误判 Agent 已完成或已验证 | 强制保留状态、验证结果、警告和原始技术信息 |
| 剪贴板包含敏感信息 | 密钥、代码或个人信息被误提交 | 仅主动读取；转换前检测并提醒；敏感内容默认不存历史 |
| 本地历史造成信息残留 | 共用设备上泄露用户输入 | 本地存储、明确入口、单条删除、全部清空和保留数量设置 |
| 全局快捷键冲突 | 无法唤起或影响其他应用 | 支持自定义快捷键并在设置时检测常见冲突 |
| 成功率难以测量 | 无法判断产品是否真正提升沟通效率 | 记录复制、轻微编辑、反馈和解释有效性等行为指标 |

## 14. 验收标准

MVP 达到以下条件时可进入封闭测试：

1. 用户可以通过菜单栏入口和可配置全局快捷键打开或隐藏悬浮窗；
2. 用户可以手动输入，或在点击按钮后主动读取当前剪贴板文本；
3. 未经用户触发时，应用不得读取剪贴板或其他应用内容；
4. 用户可以在“写给 Agent”和“看懂 Agent”之间切换；
5. “写给 Agent”能够输出 Prompt、关键假设和验收标准，且不得把推断伪装成用户要求；
6. “看懂 Agent”能够说明状态、动作、验证、风险和下一步，并保留命令、路径、错误、代码和警告；
7. 用户可以切换中文、英文、双语以及 Codex、Cursor、通用 Agent 选项；
8. 用户可以一键复制转换结果，并获得明确成功反馈；
9. 应用可以在本地展示、再次复制和删除最近历史记录；
10. 检测到疑似密钥、令牌、密码、私钥或个人信息时，应用会在转换前提醒用户；
11. MVP 不读取仓库、不自动操作或发送到 Coding Agent，也不执行任何代码或终端命令。

## 15. 待确认产品决策

以下问题不阻塞 PRD v0.1，但需要在原型开发前确定：

1. 悬浮窗采用纯 SwiftUI，还是以 AppKit 管理窗口行为、SwiftUI 构建内容界面？
2. 默认全局快捷键是什么，如何检测并处理快捷键冲突？
3. 本地历史默认保留多少条、是否提供自动过期设置？
4. 转换服务使用哪种模型，原文是否会发送到云端，以及如何向用户说明数据处理方式？
5. “看懂 Agent”是否默认输出中文，“写给 Agent”是否默认跟随用户上次选择？
6. 敏感信息提醒支持自动遮盖还是只提示用户手动处理？
7. 首期是否仅支持文本剪贴板，代码富文本和图片是否统一提示暂不支持？

**建议默认决策：** 使用“菜单栏常驻 + 全局快捷键唤起悬浮窗”的 macOS 原生形态；首期面向个人开发者，只处理文本输入；默认保留最近 20 条非敏感本地记录；任何内容读取都由用户主动触发。

## 16. 研究依据

- OpenAI, [OpenAI o1 System Card - Multilingual performance](https://openai.com/index/openai-o1-system-card/)。同一多语言 MMLU 评测中，英文成绩高于简体中文，但测试源语言为英文。
- Enomoto et al., [A Fair Comparison without Translationese: English vs. Target-language Instructions for Multilingual LLMs](https://aclanthology.org/2025.naacl-short.55/), NAACL 2025。控制翻译腔后，英文指令优势并不压倒性。
- Zhou et al., [ChLogic: Evaluating Robustness of Logical Reasoning in Chinese Expressions](https://arxiv.org/abs/2606.17905), 2026。中文回译为英文的收益因任务和模型而异。
- Li et al., [The Impact of Language Mixing on Bilingual LLM Reasoning](https://aclanthology.org/2025.emnlp-main.1654/), EMNLP 2025。在特定中英双语推理实验中，强制单语解码降低了准确率。

---

本文档是 DualTranslation 的 v0.1 产品基线。后续版本应以原型测试、转换结果采用率和用户反馈为依据更新，而不以 Prompt 长度或主观“专业感”作为主要质量标准。
